package org.build;

import javax.swing.*;

public class Grafic extends JFrame {
    public Grafic(int Wight, int Height){
        setSize(500,400);
        setDefaultCloseOperation(JFrame.ICONIFIED);
        setLocation(Wight+500,Height);
        setVisible(true);
        ;;;
    }
}